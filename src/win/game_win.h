#pragma once

#include <windows.h>

// Minimal Win32 windowed Pong implementation using GDI (no external libs)
// Provides an entry point for a windowed version of the game.

int run_win_pong(HINSTANCE hInstance, int nCmdShow);
